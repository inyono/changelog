name: JavaScript
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version:
          - 12.x
          - 14.x
    steps:
      - uses: actions/checkout@v2
      - uses: inyono/setup-yarn@main
        with:
          node-version: ${{ matrix.node-version }}
      - run: yarn build
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version:
          - 12.x
          - 14.x
    steps:
      - uses: actions/checkout@v2
      - uses: inyono/setup-yarn@main
        with:
          node-version: ${{ matrix.node-version }}
      - run: yarn lint
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version:
          - 12.x
          - 14.x
    steps:
      - uses: actions/checkout@v2
      - uses: actions/checkout@v2
      - uses: inyono/setup-yarn@main
        with:
          node-version: ${{ matrix.node-version }}
      - run: yarn test
